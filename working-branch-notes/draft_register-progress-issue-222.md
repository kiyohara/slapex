# 作業ブランチメモ

- ブランチ: `register-progress-issue-222`
- PR: (未採番)
- 最終更新: 2026-09-07

## 目的

Issue #222(`--reuse-cache` の同一ファイル再利用時に summary の copied 表記が実態とずれる)を `progress.md` の進行中タスク索引へ登録する。

## 現在の状況

`progress.md` の編集を実施。PR 作成待ち。

## 決定事項

- 登録先は「進行中タスク: review で見つかった既存挙動の修正」の表とし、ID は FU-11 とした。
  - #222 は PR #221(FU-01)の review で見つかった、リファクタリングのスコープ外にある既存挙動である。#202〜#211 と発生経路も性質も同じであり、別表を新設せず同じ索引で追跡する方が見渡しやすい。
  - 表の導入文が「PR #201 の review 補助分析で見つかった Issue #202〜#211」と限定していたため、#222 の出所が異なることを 1 文で追記した。
- 依存は PR #221 とした。同一ファイルを reused として計上する挙動が入って初めて文言のずれが発生するため、#221 の merge 前は着手できない。
- 全体の着手順は末尾に FU-11 を追加した。文言のみで件数と挙動は正しく、優先度は低いため、既存の順序は動かさない。
- 状態は `todo`、PR 欄は `-` とした。

## 次にやること

- PR を作成し、note を採番する。

## 検証

- `progress.md` を冒頭から読み、リリース履歴と完了済みフェーズの役割が壊れていないことを確認した。
- FU-11 行から Issue #222 へ辿れることを確認した。
- Issue 本文の詳細(該当行、候補文言、検証手順)は複製せず、状態・依存・参照だけを置いた。

## リスク・ブロッカー

- PR #221 も同じ FU 表の FU-01 行を編集している。行が異なるため衝突は想定しないが、merge 順によっては確認が必要になる。

## セッションログ

- 2026-09-07: Issue #222 を読み、FU 表へ FU-11 として登録。導入文と着手順の 1 行も更新した。
