# 作業ブランチメモ

- ブランチ: `claude/project-thread-qurpqc`(cloud session が指定)
- PR: #248
- 最終更新: 2026-09-25

## 目的

`maintain-progress` skill で `progress.md` を整理する。bug Issue の並行実行の案 A(2026-09-25 のユーザー判断。#203 / #205 / #207 を第1陣、#204 / #208 を第2陣とし、#191 → #206 を続ける)の第1陣と第2陣が merge され、着手順の段落と着手順の行が実態と食い違っていた。あわせて、PR #243(#204)の follow-up として起票された #245〜#247 を、`register-progress-issue` の手順で進行中タスク索引へ登録する。

PR の作成後、ユーザーの依頼で起票された follow-up の Issue #249〜#255 も、project の coordinator の依頼で同じ PR の索引へ登録する(2026-09-25)。

進捗整理と索引登録は起点 Issue を作らない運用作業のため、起点 Issue は無く、PR に `Closes` を付けない(`doc/guidelines/development-loop.md`)。

## 現在の状況

- `progress.md` の編集を終え、PR #248 を作成して本 note を採番した。その後、#249〜#255 を追加で登録した。
- ユーザーの review と merge を待つ。着手順の FU-12〜FU-14 の位置は、ユーザーが既定(#206 の後)を選んだ。#252 / #253 / #255 の扱いも、ユーザーが既定(3 件で新しい表)を選んだ。

## 決定事項

- 完了行の扱い: RF 表と FU 表は done と todo が混在するため、表ごとの圧縮はせず、done の行だけを「完了済みフェーズ(参考)」の要約 2 行へ移した(`maintain-progress` の観点 3)。要約には ID、Issue、PR を残した。
  - RF: RF-00(#188 / PR #197)、RF-08(#196 / PR #198)、RF-07(#195 / PR #199)、RF-01(#189 / PR #200)、RF-02(#190 / PR #201)。
  - FU: FU-01(#202 / PR #221)、FU-02(#203 / PR #242)、FU-03(#204 / PR #243)、FU-04(#205 / PR #241)、FU-06(#207 / PR #240)、FU-07(#208 / PR #244)、FU-11(#222 / PR #236)。
  - 行にあった「ユーザー判断で RF-03 より先行して実施」「推奨順より先行して実施」は、要約の 1 文にまとめ、経緯の参照先を PR #236 / PR #240〜#244 とした(各 PR description に並行実行と順序の例外が書いてある)。
  - FU-01 の「patch release 候補」は、完了行と一緒に消えないよう現況へ移した。v1.2.1 の後に merge した修正が未リリースであることは `git log --merges v1.2.1..origin/main` で確かめた。
  - RF-07 の「local_path表現差はcache.md参照」は RF-01 への引き継ぎで、RF-01 も完了したため残していない(PR #199 が正本)。
- 着手順: 完了した項目を除き、残りを RF-03 → FU-05 → FU-13 → FU-14 → RF-06 → RF-04 → RF-05 → FU-08 → FU-09 → FU-10 → FU-12 とした。
  - RF-03 → FU-05 は案 A の順をそのまま残した。FU-05 は #191 を必須依存とする。段落には「RF-03 の直後」と書いた。
  - #245〜#247 の ID は Issue 番号順に FU-12〜FU-14 とした(FU-01〜FU-11 と同じ付け方)。表の行は優先順に並べた(RF 表と同じく ID の番号順とは一致しない)。
  - FU-13(#246、Issue の評価で優先度 中〜低)と FU-14(#247、低)は bug で、利用者に見える表示と manifest の誤りを直す。FU 表の優先順(表示の誤り・件数と cache の整合を、API 呼び出しの無駄・edge case・後片付けより先)に従って FU-08〜FU-10 より前に置き、0056 の 2026-09-06 追記の「表示・cache の修正はリファクタリング施策の間に挟む」に合わせて RF-06 の前に置いた。FU-05 より後にしたのは、案 A の RF-03 → FU-05 を崩さないためである。
  - FU-13 → FU-14 は、#247 の本文の「#246 とは直列に進める」に従った。先後は優先度の高い #246 を先にした。必須条件ではないため依存欄には入れず、次にやることに書いた(FU-08 の旧記載「FU-04 の後(同じ関数)」と同じ扱い)。
  - FU-12(#245、設計文書だけの修正、優先度 低)は末尾に置いた(FU-11 を登録した PR #223 と同じ扱い)。#246 の後になるため、外部連携の画像の扱いを直した後の実装に合わせて文書を書ける(推測)。
  - 依存欄: FU-12 は PR #243、FU-13 / FU-14 は PR #243 と PR #244(各 Issue 本文の「依存・順序」)。いずれも merge 済みだが、依存欄は必須条件をそのまま書く扱い(RF-03 の #188〜#190 と同じ)に合わせた。
  - 位置は既定として置き、ユーザーに確認した。ユーザーは #206 の後(既定)を選んだ(2026-09-25)。ほかの選択肢は #191 の前(案 A と同じ bug 先行)と末尾(#222 と同じ)だった。
- #249〜#255 の登録(PR の作成後に追加):
  - 登録先: #249〜#251 と #254 は slapex の既存挙動の修正のため FU 表に置いた(#249 / #250 は PR #242、#251 は PR #241 の作業、#254 は PR #238 の CI で見つかった)。#252 / #253 / #255 は review cycle の skill の Issue で、FU 表の「既存挙動の修正」に当たらない。3 件とも `drive-issue-to-reviewed-pr` を触り直列に進める必要があるため、横断的に追跡する単位として新しい小さな表「進行中タスク: review cycle の skill 改善」を作った(`register-progress-issue` の手順 3)。
  - ID: FU 表は Issue 番号順に FU-15(#249)、FU-16(#250)、FU-17(#251)、FU-18(#254)。新しい表は RV-01(#252)、RV-02(#253)、RV-03(#255)。行はどちらも優先順に並べた。
  - FU-18 → FU-15 → FU-16 は FU-14 の後、RF-06 の前に置いた。ユーザーが決めた #191 → #206 → #246 → #247 を崩さないため、FU-14 より後にした。
  - FU-18(#254、優先度 中)は、RF-04(#192)が検証に使う test を直すため RF-04 より前に置く(#254 の本文)。優先度が FU-15 / FU-16(どちらも低)より高いため、その前にした。
  - FU-15(#249、件数と manifest の整合)と FU-16(#250、警告行の文言)は、FU 表の優先順と 0056 の 2026-09-06 追記の「表示・cache の修正はリファクタリング施策の間に挟む」に合わせ、FU-13 / FU-14 と同じく RF-06 の前にした。FU-15 → FU-16 は #250 の本文の「#249 の後に直列」に従った。
  - FU-17(#251、API 呼び出しの無駄、優先度 低)は、#251 の本文の「#209 と直列(順序は問わない)」に従い、FU-08 の直後に置いた。先後は登録済みの FU-08 を先にした。
  - RF-04 の次にやることに「FU-18の後」を足した。#192 の本文は #254 より前に書かれ、この順序を持たないため、索引の行から辿れるようにした。依存欄には入れない(#254 の本文は「依存なし」で、#192 は技術的には単独で着手できる)。
  - 新しい表の 3 件は、各 Issue の本文のとおり直列に進める。順序は問わないため、優先度の順(#253 中、#255 中〜低、#252 低)に並べた。依存は 3 件ともない。
  - #229 と #230 も `drive-issue-to-reviewed-pr` を触るが、新しい表には入れなかった。review cycle ではなく、1Password の失敗時手順(#229)と被委譲 skill の確認事項の報告(#230)を扱い、2 件の順序はユーザーの判断を待つ提案の段階にあるためである。新しい表の扱いと合わせてユーザーに確認し、ユーザーは 3 件で新しい表(既定)を選んだ(2026-09-25)。ほかの選択肢は、#229 / #230 も表に入れる案と、索引に載せない案だった。
  - 現況に、新しい表への案内を 1 行足した(`maintain-progress` の観点 1)。
- RF 表の前置きから、merge 済みの調査 PR を着手条件とする 2 か所(「調査PRのmerge後、」「全施策の現在の着手条件は調査PRのmergeである。」)を消した。
- FU-08 の次にやること「FU-04 の後(同じ関数)」は FU-04 の完了で意味を失ったため、Issue #209 の作業内容に合わせた。
- 現況の「8施策を追跡する」「Issue #202〜#211 も追跡する」は、件数や番号を書くと行が完了するたびに古くなるため、「未完了の施策」「review などで見つかった…Issue」とした。
- 索引に載せない open Issue: #215、#229、#230、#231、#239。agent の設定・guideline・配布の単発 Issue で、横断プランに属さない(`maintain-progress` の観点 4)。#229 と #230 は、上記の新しい表の確認でも索引の外のままとなった。
- 索引に載っていなかった完了済みの作業(cloud session 対応 #226 / PR #232、`drive-issue-to-reviewed-pr` の追加 #227 / PR #233 など)は、完了済みフェーズへ新たに足さない。本 skill の圧縮対象は `progress.md` にある表である。
- decision log は変えない。0056 の 2026-09-06 追記の順序(#207 を #191 の後に置くなど)は案 A で一部変わったが、既存の decision log は本 skill で遡って書き換えない(`maintain-progress` の観点 6)。0056 に追記するかはユーザーの判断に委ね、PR の補足に書く。

## 次にやること

- PR を作成し、`number-working-branch-note` skill で本 note を採番する。(完了)
- 着手順の FU-12〜FU-14 の位置について、ユーザーの確認を待つ。(完了。#206 の後に決まった)
- #252 / #253 / #255 を新しい表に載せる扱い(#229 / #230 を含めるか、索引に載せないか)について、ユーザーの確認を待つ。(完了。3 件で新しい表に決まった)
- review と merge はユーザーが行う。

## 検証

2026-09-25、cloud session(project の thread)で実行。

| 項目 | 結果 |
|---|---|
| open Issue / PR | GitHub MCP で open Issue 16 件、open PR 0 件を確認した。索引に残した todo 行の Issue(#191〜#194、#206、#209〜#211)はすべて open |
| 要約へ移した行 | Issue(#188〜#190、#195、#196、#202〜#205、#207、#208、#222)はすべて closed、PR(#197〜#201、#221、#236、#240〜#244)はすべて merge 済み |
| #245〜#247 | GitHub MCP で本文を読み、依存の PR #243 / PR #244 が merge 済みで、関連 PR が無いことを確かめた |
| #249〜#255 | GitHub MCP で本文を読み、7 件とも open で、関連 PR が無いことを確かめた。追加後の open Issue は 23 件、open PR は本 PR だけ。main は `c7b0f44` のまま |
| リリース | 最新の GitHub Release は v1.2.1(2026-09-03)で、リリース履歴と一致する。v1.2.1 の後の merge commit は 24 件で、PR #221 を含む |
| 参照整合性 | `progress.md` を冒頭から読み、リリース台帳と進行中タスクの索引の役割、リリース履歴の見出しと列、完了済みフェーズの構成を変えていないことを確かめた。`progress.md` を参照する AGENTS.md、doc/README.md、doc/design/README.md、guideline、skill、rule の前提は変わらない |
| `git diff --check` | 出力なし |

コード変更が無いため test は実行していない。

## リスク・ブロッカー

- なし。着手順の FU-12〜FU-14 の位置は、ユーザーが既定を選んだ。#252 / #253 / #255 の扱いも、ユーザーが既定(3 件で新しい表)を選んだ。

## セッションログ

- 2026-09-25: `maintain-progress` で RF 表と FU 表の完了行を要約へ移し、着手順を実態に合わせ、#245〜#247 を FU-12〜FU-14 として登録した。
- 2026-09-25: PR #248 を作成し、`number-working-branch-note` で本 note を採番した。
- 2026-09-25: FU-13 / FU-14 の位置をユーザーに確認し、#206 の後(既定)に決まった。`progress.md` は変えていない。
- 2026-09-25: coordinator の依頼で、起票された #249〜#255 を登録した。#249〜#251 / #254 を FU-15〜FU-18 として FU 表へ、#252 / #253 / #255 を RV-01〜RV-03 として新しい表へ置いた。
- 2026-09-25: #252 / #253 / #255 の扱いをユーザーに確認し、3 件で新しい表(既定)に決まった。`progress.md` は変えていない。
